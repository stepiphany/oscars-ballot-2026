# Optional: Add these for Supabase (multi-device real-time sync)
# Without these, the app runs in local-only mode (single device / same browser)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
